# huffman
A Haskell implementation of the Huffman coding algorithm

This is an experimental code, still under development.

For the moment, only the information content is printed by the program.

Run `make` to build the executable `huffman`. Run it this way
on the sample text:

    ./huffman < prefaceNewton.txt
